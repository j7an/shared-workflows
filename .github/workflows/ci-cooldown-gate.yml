name: Dependency Cool-Down Gate

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  statuses: write
  issues: write

jobs:
  gate:
    uses: ./.github/workflows/dependency-cooldown-gate.yml
