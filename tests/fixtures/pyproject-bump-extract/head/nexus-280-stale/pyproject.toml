# stale: one extra line shifts every hunk start
[project]
name = "nexus-mcp"
dynamic = ["version"]
description = "MCP server that delegates tasks to coding agents (Claude, Codex)"
readme = "README.md"
authors = [
    { name = "j7an", email = "j7an@users.noreply.github.com" }
]
license = "MIT"
keywords = [
    "ai-agents",
    "ai-orchestration",
    "claude-code",
    "claude-desktop",
    "claudecode",
    "cli-tools",
    "codex",
    "codex-cli",
    "fastmcp",
    "mcp",
    "mcp-server",
    "model-context-protocol",
    "multi-agent",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed",
]
requires-python = ">=3.12"
dependencies = [
    "anyio>=4.15.1,<5.0",
    "fastmcp>=4.0.10,<4.1",
    "pydantic>=2.12.5,<3.0",
]

[project.optional-dependencies]
claude = ["claude-agent-sdk>=0.2.158,<0.3"]
codex = ["openai-codex>=0.157.1"]
all = ["nexus-mcp[claude,codex]"]

[project.urls]
Homepage = "https://github.com/j7an/nexus-mcp"
Repository = "https://github.com/j7an/nexus-mcp"
Issues = "https://github.com/j7an/nexus-mcp/issues"

[project.scripts]
nexus-mcp = "nexus_mcp.__main__:main"

[build-system]
requires = ["hatchling>=1.28.0", "hatch-vcs>=0.4.0"]
build-backend = "hatchling.build"
# Note: hatchling>=1.28.0 auto-discovers src-layout; no [tool.hatch.build.targets.wheel] needed

[tool.hatch.version]
source = "vcs"
raw-options = { version_scheme = "no-guess-dev" }

[dependency-groups]
dev = [
    "diff-cover>=10.2.0,<11.0",
    "mypy>=1.19.1,<2.0",
    "pre-commit>=4.5.1,<5.0",
    "pytest>=9.0.2,<10.0",
    "pytest-asyncio>=1.3.0,<2.0",
    "pytest-cov>=7.0.0,<8.0",
    "ruff>=0.16.8,<1.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    # Core linting
    "E",    # pycodestyle errors
    "F",    # Pyflakes (undefined names, unused imports)
    "I",    # isort (import sorting)
    "W",    # pycodestyle warnings

    # Modern Python practices
    "UP",   # pyupgrade (modernize syntax for target Python version)
    "FA",   # flake8-future-annotations (PEP 563 compliance)

    # Code quality
    "B",    # flake8-bugbear (detect likely bugs and design problems)
    "C4",   # flake8-comprehensions (better list/dict/set comprehensions)
    "SIM",  # flake8-simplify (simplification opportunities)
    "RET",  # flake8-return (improve return statements)

    # Import management
    "ICN",  # flake8-import-conventions (enforce import aliases, e.g., pd for pandas)
    "TID",  # flake8-tidy-imports (ban relative imports, enforce import style)
    "TC",   # flake8-type-checking (optimize TYPE_CHECKING blocks)

    # String handling
    "ISC",  # flake8-implicit-str-concat (flag implicit string concatenation)

    # Pathlib migration
    "PTH",  # flake8-use-pathlib (prefer pathlib over os.path)

    # Documentation
    "TD",   # flake8-todos (require issue links in TODO comments)

    # Library-specific
    "NPY",  # NumPy-specific rules (better array operations)
]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true

[tool.pytest.ini_options]
addopts = "-m 'not integration'"
asyncio_mode = "auto"  # Recommended by FastMCP: eliminates need for @pytest.mark.asyncio decorators
asyncio_default_fixture_loop_scope = "function"  # Avoids deprecation warning in pytest-asyncio 1.x (will be default in v2.0)
testpaths = ["tests"]  # Explicit test discovery
filterwarnings = [
    # Fail on leaked handles such as unclosed sqlite3 connections. Leaks reported during
    # garbage collection surface as unraisable-exception warnings, so escalate both.
    "error::ResourceWarning",
    "error::pytest.PytestUnraisableExceptionWarning",
]
markers = [
    "integration: tests that call real CLI subprocesses",
    "slow: tests that take >1 second",
    "e2e: tests that exercise the full MCP protocol stack via in-process Client",
]

[tool.coverage.run]
source = ["nexus_mcp"]
branch = true
relative_files = true
omit = [
    "*/tests/*",
    "*/__main__.py",
]

[tool.coverage.report]
fail_under = 90
show_missing = true
exclude_also = [
    "if TYPE_CHECKING:",
    'if __name__ == .__main__.:',
    "@(abc\\.)?abstractmethod",
    "raise NotImplementedError",
    "class .*\\bProtocol\\):",
    "\\.\\.\\.",
]

[tool.uv]
exclude-newer = "5 days"

# Temporary exception for FastMCP 4.0.10 / claude-agent-sdk 0.2.158 (v2 baseline).
# Remove after 2026-09-30, when the five-day cutoff includes these releases.
[tool.uv.exclude-newer-package]
fastmcp = "2026-09-26T00:00:00Z"
fastmcp-slim = "2026-09-26T00:00:00Z"
claude-agent-sdk = "2026-09-24T00:00:00Z"
# Temporary exception for openai-codex 0.157.1 (v2 Codex backend baseline).
# Remove after 2026-10-01, when the five-day cutoff includes this release.
openai-codex = "2026-09-27T00:00:00Z"
openai-codex-cli-bin = "2026-09-27T00:00:00Z"
